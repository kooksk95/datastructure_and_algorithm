#include <bits/stdc++.h>
using namespace std;


// 문제: 정수 배열의 연속 구간 중 합이 가장 큰 구간을 찾아라
// ** 예시 만들기가 귀찮으므로 언젠가 해당 문제를 찾으면 추가

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    /*
        1. get prefix sum
        2. get max interval by
            for(i in 1 ~ N-1){
                for(j in i+1 ~ N){
                    update max
                }
            }

    */

    return 0;
}